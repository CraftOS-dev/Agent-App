# Playground

The living reference (AG-UI dojo pattern): a real Agent App scaffolded from a blueprint, scaffolded with `agent-app scaffold`, operable via the `a2app` client and visually in the browser, plus the adapter feature matrix (which capability flags each adapter/blueprint implements, backed by conformance results). Lands after `framework/cli` and the first blueprint.
