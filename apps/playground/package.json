{
  "name": "@a2app/playground",
  "version": "0.1.0",
  "private": true,
  "description": "A runnable reference Agent App (contacts) used as a live example and an end-to-end boot test",
  "license": "MIT",
  "type": "module",
  "main": "./dist/server.js",
  "engines": { "node": ">=20" },
  "dependencies": {
    "@a2app/adapter-core": "workspace:*",
    "@a2app/rules": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^20.19.0"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "start": "node dist/server.js",
    "test": "echo \"no tests\" && exit 0"
  }
}
