{
  "name": "@a2app/integration-claude-code",
  "version": "0.1.0",
  "private": true,
  "description": "Agent App Framework MCP server for Claude Code: exposes the framework build+operate tools (the agent-app and a2app CLIs) over the Model Context Protocol",
  "license": "MIT",
  "type": "module",
  "main": "./dist/index.js",
  "bin": { "a2app-mcp": "./dist/index.js" },
  "engines": { "node": ">=20" },
  "dependencies": {
    "@a2app/integration-starter": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^20.19.0"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "echo \"no tests\" && exit 0"
  }
}
