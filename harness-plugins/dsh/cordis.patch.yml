# Profile patch layer for the Agent App Framework bundle.
# A dsh bundle's substance is this file; the manifest points here via
# `dsh.bundle.patch`. One insert row mounts the host plugin by its package name.
- insert:
    - id: agent-app
      name: dsh-agent-app
