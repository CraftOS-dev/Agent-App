{
  "name": "@a2app/integration-openclaw",
  "version": "0.1.0",
  "private": true,
  "description": "Agent App Framework plugin for OpenClaw: agent-app CLI passthrough + Control-UI entry form + the six framework skills, driving the agent-app and a2app CLIs",
  "license": "MIT",
  "type": "module",
  "main": "./dist/index.js",
  "engines": { "node": ">=20" },
  "devDependencies": {
    "@a2app/integration-starter": "workspace:*",
    "esbuild": "^0.25.0"
  },
  "scripts": {
    "build": "node scripts/build.mjs",
    "test": "echo \"no tests yet\" && exit 0"
  }
}
