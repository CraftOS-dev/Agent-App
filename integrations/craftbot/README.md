# CraftBot integration

Plugin that lets the CraftBot harness build, display, and operate Agent Apps: it registers agent tools that wrap the `a2app` CLI, mounts the framework skills, and embeds a running app in CraftBot's iframe pool. Harnesses without a plugin use the universal [skills](../../skills/) instead (the app opens in the browser).
