{
  "name": "@a2app/integration-dsh",
  "version": "0.1.0",
  "private": true,
  "description": "Agent App Framework bundle for deepseek-harness (host tools + jobs + a client module that iframes the app + shipped skills)",
  "license": "MIT",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "engines": { "node": ">=20" },
  "dependencies": {
    "@a2app/integration-starter": "workspace:*"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "echo \"no tests yet\" && exit 0"
  }
}
