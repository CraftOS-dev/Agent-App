{
  "name": "@a2app/integration-starter",
  "version": "0.1.0",
  "private": true,
  "description": "Copy-to-create template for an Agent App Framework plugin for a new agent harness: expose the CLI as a tool, ship the skills, offer embedded display",
  "license": "MIT",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "engines": { "node": ">=20" },
  "devDependencies": {
    "@types/node": "^20.19.0"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "echo \"no tests yet\" && exit 0"
  }
}
