{
  "name": "@a2app/integration-openclaw",
  "version": "0.1.0",
  "private": true,
  "description": "Agent App Framework plugin for OpenClaw (tools + CLI + Control-UI tab + shipped skills)",
  "license": "MIT",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "engines": { "node": ">=20" },
  "dependencies": {
    "@a2app/integration-starter": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^20.19.0"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "echo \"no tests yet\" && exit 0"
  }
}
