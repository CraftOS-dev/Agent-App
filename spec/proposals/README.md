# Spec proposals

Change proposals targeting the **next** protocol version. Released `vX_Y/` snapshots are frozen; all evolution flows through here.

One markdown file per proposal: motivation, wire-level change (schema diff), compatibility impact, conformance-test additions. Accepted proposals land in the next version directory together.
