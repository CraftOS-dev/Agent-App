{
  "name": "rails-vue-agent-app-ui",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "The Vue 3 View of an Agent App scaffolded from blueprint-rails-vue. `npm run build` compiles it into ../public, which Rails serves.",
  "engines": { "node": ">=20" },
  "dependencies": {
    "vue": "^3.5.0"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^6.0.0",
    "vite": "^8.3.0"
  },
  "scripts": {
    "build": "vite build",
    "dev": "vite"
  }
}
